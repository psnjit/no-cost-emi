import React from "react";
import "./Header.css"; // Import the external CSS file

export const Header = () => {
  return (
    <header>
      <h1 className="header-title">Clear EMI</h1>
    </header>
  );
};